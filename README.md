TUI typing test (ttt)
pronounciation: two-e typing test

simple one command compilation:
```C
gcc -Wall -Wextra -O2 -march=native main.c -o typing_test -lncurses
```

send feature recommendations to: pira8@tuta.io
